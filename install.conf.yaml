- defaults:
    link:
      relink: true
- clean: ['~']
- link:
    ~/.emacs.d:
      create: true
      path: links/emacs
      force: true
- shell:
    - [git submodule update --init --recursive, Installing/updating submodules]
